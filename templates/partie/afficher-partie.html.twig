{% extends 'base.html.twig' %}


{% block body %}
            <div class="container-fluid" id="plateau">
                <div class="row mainJ2">
                    {% for idCarte in  partie.mainJ2 %}
                        <div class="col-sm-1 carteMain {{ cartes[idCarte].couleur }}">{{ cartes[idCarte].puissance }}</div>
                    {% endfor %}
                </div>
                <div class="row tuilesJ2">
                    {% for i in 1..9 %}
                        <div class="tuile">-</div>
                    {% endfor %}
                </div>
                <div class="row cartesJ2">
                    cartes posées (terrain J2)
                </div>
                <div class="row tuiles">
                    {% for i in 1..9 %}
                        <div class="tuile">Tuile {{ i }}</div>
                    {% endfor %}
                </div>
                <div class="row cartesJ1">
                    {% for j in 1..3 %}
                        <div class="ligne">
                            {% for i in 1..9 %}
                                <div class="carte droppable">-</div>
                            {% endfor %}
                        </div>
                    {% endfor %}
                </div>
                <div class="row tuilesJ2">
                    {% for i in 1..9 %}
                        <div class="tuile ">-</div>
                    {% endfor %}
                </div>
                <div class="row mainJ1"><br>
                    {% for idCarte in  partie.mainJ1 %}
                        <div class="col-sm-1 carteMain draggable {{ cartes[idCarte].couleur }}">{{ cartes[idCarte].puissance }}</div>
                    {% endfor %}
                </div>

            </div>

{% endblock %}



{% block javascripts %}
    {{ parent() }}
    <script>
        $( function() {
            $( ".draggable" ).draggable({ revert: 'invalid' });
            $( ".droppable" ).droppable({
                accept: ".draggable",
                classes: {
                    "ui-droppable-active": "ui-state-active",
                    "ui-droppable-hover": "ui-state-hover"
                },
                drop: function( event, ui ) {
                    $( this )
                        .addClass( "ui-state-highlight" )
                }
            });
        } );
    </script>
{% endblock %}

